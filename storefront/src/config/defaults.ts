// Centralized default values for the storefront

// Free shipping threshold in cents (25000 = $250)
export const DEFAULT_FREE_SHIPPING_THRESHOLD = 25000;

// Default currency
export const DEFAULT_CURRENCY = 'USD';

// Default timeout values (in milliseconds)
export const API_TIMEOUT = 30000;
export const DEBOUNCE_DELAY = 300;
export const SEARCH_DEBOUNCE_DELAY = 500;

// Pagination defaults
export const DEFAULT_PAGE_SIZE = 12;
export const MAX_PAGE_SIZE = 100;

// Order defaults
export const DEFAULT_PAYMENT_TERMS = 'net_30';
export const ORDERS_PER_PAGE = 10;

// Cart constants
export const CART_DEBOUNCE_MS = 1000;
